(ns aoc.2020.day-11
  [:require
   [aoc.utils :as u]
   [clojure.string :as s]])

(def data
  (->> (slurp "resources/2022/day-11.txt")))

(defn part-one
  ([] (part-one data))
  ([input]
   nil))

(defn part-two
  ([] (part-two data))
  ([input]
   nil))
